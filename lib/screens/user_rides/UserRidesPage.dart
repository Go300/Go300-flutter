import 'package:flutter/material.dart';

class UserRidesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text("Мои поездки", style: TextStyle(fontSize: 32.0, fontWeight: FontWeight.w400)),
      ),
      body: Text("404. NOT FOUND."),
    );
  }
}